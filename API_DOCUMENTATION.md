# تطبيق تاكسي أكوادرا - نظام الرحلات المتكامل

## 🚀 الميزات الجديدة

### نظام الرحلات المكتملة والملغية
- ✅ عرض جميع الرحلات المكتملة مع التفاصيل الكاملة
- ❌ عرض الرحلات الملغية مع سبب الإلغاء
- 📊 إحصائيات متقدمة للرحلات
- 🔄 مزامنة تلقائية مع قاعدة البيانات PostgreSQL
- 📱 واجهة مستخدم محسّنة للعرض والفلترة

## 🛠️ التقنيات المستخدمة

### Backend
- **Node.js** - بيئة التشغيل
- **Express.js** - إطار عمل الويب
- **PostgreSQL** - قاعدة البيانات
- **Railway** - استضافة قاعدة البيانات

### Frontend
- **HTML5, CSS3** - البنية والتصميم
- **Tailwind CSS** - مكتبة التصميم
- **JavaScript (Vanilla)** - البرمجة
- **Leaflet.js** - الخرائط التفاعلية

## 📦 التثبيت والإعداد

### 1. تثبيت الحزم
```bash
npm install
```

### 2. إعداد قاعدة البيانات
```bash
node setup-db.js
```

### 3. تشغيل السيرفر
```bash
npm start
# أو للتطوير
npm run dev
```

### 4. فتح التطبيق
افتح المتصفح على: `http://localhost:3000`

## 🔌 API Endpoints

### الرحلات

#### الحصول على جميع الرحلات
```
GET /api/trips?status=all&limit=50&offset=0
```

#### الحصول على الرحلات المكتملة
```
GET /api/trips/completed
```

#### الحصول على الرحلات الملغية
```
GET /api/trips/cancelled
```

#### الحصول على رحلة محددة
```
GET /api/trips/:id
```

#### إنشاء رحلة جديدة
```
POST /api/trips
Content-Type: application/json

{
  "pickup_location": "شارع التحلية، الرياض",
  "dropoff_location": "العليا مول",
  "car_type": "economy",
  "cost": 45.50,
  "distance": 10.5,
  "duration": 20,
  "payment_method": "card"
}
```

#### تحديث حالة الرحلة
```
PATCH /api/trips/:id/status
Content-Type: application/json

{
  "status": "completed",
  "rating": 5
}
```

#### الحصول على إحصائيات الرحلات
```
GET /api/trips/stats/summary
```

### السائقين

#### الحصول على السائقين المتاحين
```
GET /api/drivers?status=online
```

## 📊 قاعدة البيانات

### جدول الرحلات (trips)
- `id` - معرف الرحلة
- `user_id` - معرف المستخدم
- `driver_id` - معرف السائق
- `pickup_location` - موقع الانطلاق
- `dropoff_location` - موقع الوصول
- `car_type` - نوع السيارة (economy, family, luxury)
- `cost` - التكلفة
- `distance` - المسافة
- `duration` - المدة
- `payment_method` - طريقة الدفع (cash, card, wallet)
- `status` - الحالة (completed, cancelled, ongoing)
- `rating` - التقييم (1-5)
- `driver_name` - اسم السائق
- `created_at` - تاريخ الإنشاء
- `completed_at` - تاريخ الإكمال
- `cancelled_at` - تاريخ الإلغاء

## 🧪 الاختبار

### اختبار API
```bash
npm test
```

### اختبار يدوي
```bash
# Health Check
curl http://localhost:3000/api/health

# الرحلات المكتملة
curl http://localhost:3000/api/trips/completed

# الرحلات الملغية
curl http://localhost:3000/api/trips/cancelled

# الإحصائيات
curl http://localhost:3000/api/trips/stats/summary
```

## 📱 استخدام التطبيق

### 1. اختيار الدور
- راكب (Passenger)
- سائق (Driver)
- مدير (Admin)

### 2. عرض الرحلات
1. افتح القائمة الجانبية
2. اختر "رحلاتي"
3. استخدم الفلاتر:
   - **الكل** - عرض جميع الرحلات
   - **مكتملة** - الرحلات المكتملة فقط
   - **ملغية** - الرحلات الملغية فقط

### 3. تفاصيل الرحلة
- انقر على أي رحلة لعرض التفاصيل الكاملة
- عرض المسار والتكلفة والتقييم
- معلومات السائق والسيارة

## 🔐 المتغيرات البيئية

ملف `.env`:
```env
DATABASE_URL=postgresql://postgres:password@host:port/database
PORT=3000
```

## 📈 الإحصائيات المتاحة

- إجمالي عدد الرحلات
- الرحلات المكتملة
- الرحلات الملغية
- إجمالي المبلغ المنفق
- متوسط التقييم
- إجمالي المسافة المقطوعة

## 🚀 النشر

### Railway
```bash
# ربط المشروع بـ Railway
railway link

# نشر التطبيق
railway up
```

## 📝 ملاحظات

- التطبيق يدعم الوضع المزدوج: API + LocalStorage
- في حالة عدم توفر API، يتم التبديل تلقائياً إلى LocalStorage
- جميع البيانات محمية ومشفرة
- يدعم اللغة العربية بالكامل

## 🐛 استكشاف الأخطاء

### السيرفر لا يعمل
```bash
# تحقق من البورت
lsof -i :3000

# أوقف العملية القديمة
kill -9 $(lsof -t -i:3000)

# أعد تشغيل السيرفر
npm start
```

### قاعدة البيانات لا تتصل
```bash
# تحقق من متغيرات البيئة
cat .env

# أعد إعداد قاعدة البيانات
node setup-db.js
```

## 📞 الدعم

للمساعدة والاستفسارات، يرجى فتح Issue في GitHub.

---

تم التطوير بواسطة GitHub Copilot ❤️
