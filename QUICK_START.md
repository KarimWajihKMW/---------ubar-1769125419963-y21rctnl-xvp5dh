# 🚀 تشغيل التطبيق - دليل سريع

## الخطوات السريعة

### 1️⃣ تثبيت الحزم (مرة واحدة)
```bash
npm install
```

### 2️⃣ إعداد قاعدة البيانات (مرة واحدة)
```bash
node setup-db.js
```

### 3️⃣ تشغيل السيرفر
```bash
npm start
```

بديل (يقرأ `.env` تلقائيًا):
```bash
./scripts/run-dev.sh
```

### 4️⃣ فتح التطبيق
افتح المتصفح على: **http://localhost:3000**

---

## 🎯 استخدام ميزة الرحلات

### الوصول إلى الرحلات:
1. افتح التطبيق
2. اختر دور "راكب"
3. افتح القائمة الجانبية (☰)
4. اضغط على "رحلاتي"

### الفلترة:
- **الكل** - عرض جميع الرحلات
- **مكتملة** - الرحلات المكتملة فقط ✅
- **ملغية** - الرحلات الملغية فقط ❌

### عرض التفاصيل:
- اضغط على أي رحلة لعرض التفاصيل الكاملة
- ستجد: المسار، التكلفة، التقييم، معلومات السائق

---

## 📊 الإحصائيات المتاحة

في صفحة الرحلات، ستجد:
- **إجمالي الرحلات** 🚗
- **المبلغ المنفق** 💰
- **متوسط التقييم** ⭐

---

## 🔧 استكشاف الأخطاء

### إذا كان السيرفر لا يعمل:
```bash
# أوقف العملية القديمة
killall node

# أعد تشغيل السيرفر
npm start
```

### إذا كانت قاعدة البيانات فارغة:
```bash
node setup-db.js
```

---

## 📝 ملاحظات مهمة

✅ التطبيق يعمل على البورت 3000  
✅ قاعدة البيانات على Railway  
✅ البيانات التجريبية متاحة للاختبار  
✅ يدعم الوضع المزدوج (API + LocalStorage)  

---

## 🔐 Security + JWT + Wallet (حسب الخطة)

### Environment Variables
- `DATABASE_URL` مطلوب
- `JWT_SECRET` مهم جدًا عشان التوكن يفضل ثابت بعد إعادة تشغيل السيرفر
- `JWT_ACCESS_TTL_SECONDS` اختياري (افتراضي 86400)

### Token Usage (Web)
- التوكن بيتخزن في `SafeStorage` بالمفتاح `akwadra_token`
- كل طلبات الـ API بتتبعت بـ `Authorization: Bearer <token>`

### Wallet endpoints
- `GET /api/wallet/me/balance`
- `GET /api/wallet/me/transactions?limit=&offset=`
- `POST /api/admin/wallet/transaction` (admin فقط)

### Tests (قبل أي commit)
```bash
npm test
npm run build
```

---

## 🎉 جاهز للاستخدام!

التطبيق الآن جاهز وكل الميزات تعمل بشكل صحيح.

لمزيد من التفاصيل، راجع ملف [API_DOCUMENTATION.md](API_DOCUMENTATION.md)
